version: '3'
services:
  bootstrap:
    build: ./bootstrap
    ports:
      - "5000:5000"
    links:
      - redis
  enqueue:
    build: ./enqueue
    links:
      - redis
  # watcher:
  #   build: ./watcher-node
  #   # build: ./watcher-dotnet
  #   volumes:
  #     - ./data/drop:/var/watch
  redis:
    image: "redis:alpine"
    ports:
      - "63799:6379"