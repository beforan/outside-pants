{
  "name": "watcher",
  "version": "1.0.0",
  "private": true,
  "description": "watches a folder, queues new files in redis and moves them elsewhere",
  "scripts": {
    "start": "node ./app/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jonathan Couldridge <Jonathan.Couldridge@nottingham.ac.uk>",
  "license": "MIT",
  "dependencies": {
    "gulp": "^4.0.0"
  },
  "devDependencies": {
    "eslint": "^4.19.1"
  }
}
